window.__NAV_CONFIG__ = {
  site: {
    title: "${NAV_TITLE}",
    description: "${NAV_DESCRIPTION}",
    theme: "${NAV_THEME}"
  },
  searchEngines: ${NAV_SEARCH_ENGINES_JSON},
  groups: ${NAV_GROUPS_JSON}
};
